

export const metadata = {
    title: "PDF Reader",
    description: "PDF Reader for my pesonal collection of eBooks.",
  };
  
  export default function RootLayout({ children }) {
    return (
        <div>
          {children}
        </div>
    );
  }
  