

export const metadata = {
  title: "Dashboard",
  description: "User profile for this ebooks application.",
};

export default function RootLayout({ children }) {
  return (
      <div>
            {children}
      </div>
  );
}
